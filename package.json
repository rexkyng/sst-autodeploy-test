{
  "name": "openauth",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "generate:api": "cd packages/tsoa && npm run tsoa:build && cd ../..",
    "generate:client": "cd packages/api && npm run generate && cd ../..",
    "dev": "npm run generate:api && npm run generate:client && sst dev"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22",
    "typescript": "^5"
  },
  "dependencies": {
    "sst": "^3"
  },
  "private": true
}